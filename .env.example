# Required
OPENAI_API_KEY=sk-...
APP_PASSWORD=choose-a-strong-password

# Optional
# Comma-separated list; first entry is the default language for code answers
PREFERRED_LANGUAGES=C#, JavaScript
# Overrides the session-cookie signing secret (otherwise derived from APP_PASSWORD)
# SESSION_SECRET=
# PORT=3000
